# Build artifacts
build/
notes

# Per-machine application configuration (lives next to the binary)
notes.ini

# Development sandbox (make run-dev): its own ini and throwaway database
dev/

# macOS noise
.DS_Store

# Packaging artifacts
dist/
